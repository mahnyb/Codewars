#include <vector>
using namespace std; 

int findSmallest(vector <int> list){
  int min = list [0];
  for (int i = 0; i < list.size(); i++){
    if (list.at(i) <=  min){
      min = list.at(i);
    }
  }
   return min;
}
